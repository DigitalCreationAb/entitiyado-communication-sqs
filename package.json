{
    "name": "@digitalcreation/lambda-actors-sqs",
    "version": "0.3.0",
    "license": "MIT",
    "devDependencies": {
        "rimraf": "^3.0.2",
        "rollup": "^2.39.1",
        "rollup-plugin-dts": "^2.0.1",
        "tslint": "^6.1.3",
        "typescript": "^4.1.5"
    },
    "dependencies": {
        "@digitalcreation/aws-lambda-actors": "^0.10.0",
        "ts-md5": "^1.2.7",
        "aws-sdk": "^2.849.0",
        "@types/aws-lambda": "^8.10.72"
    },
    "files": [
        "index.js",
        "index.d.ts"
    ],
    "scripts": {
        "build": "yarn run clean && tsc && rollup -c",
        "push": "yarn run build && npm publish . --scope digitalcreation",
        "clean": "rimraf dist && rimraf build && rimraf index.js && rimraf index.d.ts"
    }
}
